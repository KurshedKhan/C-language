#include<stdio.h>
int main()
{
    int age=18;
    if( age <=18){
    printf("you are not elegible to vote %d", age);
    }
    else{
        printf("you are elegible to vote %d", age);
    }
    return 0;
}