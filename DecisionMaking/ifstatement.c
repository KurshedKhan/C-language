#include <stdio.h>

/*
    if(condition) 
    {
        // if body
        // Statements to execute if condition is true
    }

*/

int main(){

    int a = 10;

    if ( a < 18)
    {
        printf(" a is less than 18 : %d\n",a);
    }
    printf(" a is greater than 18");

    return 0;
}