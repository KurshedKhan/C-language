#include <stdio.h>
#include <string.h>

int main(){

    char str[20];
    printf("Enter value for string:");
    scanf("%s", str);

    printf("\nValue stored in string is:%s", str);

    return 0;
}