# `bool` in C

In C, the `bool` (Boolean) type was introduced in the **C99** standard via the `<stdbool.h>` header.

## ✅ How to Use `bool`

```c
#include <stdbool.h>

int main() {
    bool isActive = true;

    if (isActive) {
        // Code to execute if true
    }

    return 0;
}
